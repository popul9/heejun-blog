---
import { type CollectionEntry, getCollection, render } from "astro:content";
import BlogPost from "../../../layouts/BlogPost.astro";

export async function getStaticPaths() {
    const posts = await getCollection("roamer");
    const koPosts = posts.filter(
        (post) => post.id.endsWith("/ko") || !post.id.includes("/")
    );
    return koPosts.map((post) => {
        const basePath = post.id.replace(/\/ko$/, "");
        return {
            params: { slug: basePath },
            props: post,
        };
    });
}
type Props = CollectionEntry<"roamer">;

const post = Astro.props;
const { Content } = await render(post);
---

<BlogPost {...post.data}>
    <Content />
</BlogPost>
